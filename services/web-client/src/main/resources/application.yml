server:
  port: 8082

spring:
  application:
    name: web-client
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: web-app
            # PUBLIC client -> no secret. Setting auth method to "none" makes
            # Spring Security automatically use PKCE for the code exchange.
            client-authentication-method: none
            authorization-grant-type: authorization_code
            scope: openid,profile,email
            # Default callback Spring listens on. Must be a registered redirect URI.
            redirect-uri: "{baseUrl}/login/oauth2/code/keycloak"
        provider:
          keycloak:
            # Discovery drives everything (auth, token, jwks, userinfo, end-session).
            # Canonical Keycloak URL (see product-service note). Override via env.
            issuer-uri: ${KEYCLOAK_ISSUER_URI:http://host.docker.internal:8085/realms/demo}
            user-name-attribute: preferred_username

# Downstream API this client calls with the user's access token.
# On-host default is localhost; in compose it's the service name product-service.
product-service:
  url: ${PRODUCT_SERVICE_URL:http://localhost:8081}

logging:
  level:
    org.springframework.security: INFO
